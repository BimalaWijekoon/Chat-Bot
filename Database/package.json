{
  "name": "server",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "build": "echo 'No build step required for server' && exit 0",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "description": "Backend server for handling user sign-up and authentication",
  "dependencies": {
    "bcrypt": "^5.1.0",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.19.1",
    "helmet": "^7.0.0",
    "mongoose": "^8.2.2",
    "nodemailer": "^6.9.15",
    "twilio": "^5.3.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
